# Config de usuario (no versionar)
config.yaml
.env.local
jota-env.sh

# Entornos virtuales
.venv/
__pycache__/
*.pyc
*.pyo

# Dispositivos (solo example.env se versiona)
devices/*.env
!devices/example.env

# Cache de APKs del bootstrap (local del Mac)
~/.jota-voice/

# pytest
.pytest_cache/

# Carpeta de Claude Code (settings locales con permisos y rutas personales)
.claude/

# Planes y specs de desarrollo (internos, no para el repo público)
docs/superpowers/
